services:
  app:
    restart: unless-stopped
    build:
      context: ./app
    env_file:
      - .env.app
    ports:
      - 80:80
    depends_on:
      - db
      - redis

  db:
    image: postgres
    restart: unless-stopped
    env_file:
      - .env.db

  redis:
    image: redis
